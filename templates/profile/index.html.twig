{% extends 'base.html.twig' %}

{% block title %}
{{ 'menu.profile'|trans }}
{% endblock %}

{% block body %}

<div class="bg-gray-100 h-full flex justify-center ">
    <div class="w-full h-full max-w-lg">
        <div class="bg-white shadow-lg rounded p-8">
            <h1 class="text-2xl mb-10 font-bold">{{ 'profile.hey'|trans }} {{user.lastName}}. {{ 'profile.ready'|trans }}</h1>
            <p>{{ 'profile.answer'|trans }}</p>
            <hr class="my-4">
            <p>{{ 'profile.apply'|trans }}</p>
            <hr class="my-4">
            <p>{{ 'profile.getpaid'|trans }} </p>
            <hr class="my-4">
            <a href="{{path('app_profile_account')}}" class="bg-blue-500 mb-10 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">{{ 'profile.start'|trans }}</a>
        </div>
    </div>
</div>


{% endblock %}
